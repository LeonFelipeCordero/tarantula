pluginManagement {
    repositories {
        mavenCentral()

        maven { url 'https://plugins.gradle.org/m2/' }
    }
}
rootProject.name = 'tarantula-sample'

include 'tarantula'
project(':tarantula').projectDir = file('../tarantula')
